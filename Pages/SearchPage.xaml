<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SleepHaven.SearchPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SleepHaven"
    Title="Search"
    Shell.TabBarIsVisible="False">

    <Grid RowDefinitions="Auto,*" Padding="20,18,20,24">
        <VerticalStackLayout Spacing="4" Margin="0,0,0,16">
            <Label Text="Search results" Style="{StaticResource PageTitleStyle}" />
            <Label x:Name="SearchResultTitle" Style="{StaticResource BodyStyle}" />
        </VerticalStackLayout>

        <CollectionView x:Name="ResultsCollectionView"
                        Grid.Row="1"
                        SelectionMode="Single"
                        SelectionChanged="OnProductSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="local:Product">
                    <Grid Padding="4,8" ColumnDefinitions="88,*" ColumnSpacing="14">
                        <Border StrokeThickness="0" StrokeShape="RoundRectangle 14" HeightRequest="88">
                            <Image Source="{Binding ThumbnailUrl}"
                                   Aspect="AspectFill"
                                   SemanticProperties.Description="{Binding Name}" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                            <Label Text="{Binding Name}" Style="{StaticResource ProductNameStyle}" />
                            <Label Text="{Binding Category}" Style="{StaticResource CaptionStyle}" />
                            <Label Text="{Binding Price}" Style="{StaticResource PriceStyle}" />
                        </VerticalStackLayout>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <VerticalStackLayout x:Name="EmptyStateContainer"
                             Grid.Row="1"
                             IsVisible="False"
                             VerticalOptions="Center"
                             Padding="28"
                             Spacing="6">
            <Label Text="No matching products" Style="{StaticResource SectionTitleStyle}" HorizontalTextAlignment="Center" />
            <Label Text="Try a material, product type, or season." Style="{StaticResource BodyStyle}" HorizontalTextAlignment="Center" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
